name: Release → Jira

on:
  release:
    types: [published]

jobs:
  propagate-release:
    runs-on: ubuntu-slim
    steps:
      - name: Propagate release on Jira
        uses: Alfresco/alfresco-build-tools/.github/actions/jira-propagate-release@v15.9.1
        with:
          github-release-prefix: "v"
          jira-project-key: "OPSEXP"
          jira-release-prefix: "Alfresco Charts - content services "
          jira-token: ${{ secrets.JIRA_TOKEN }}
          jira-url: ${{ vars.JIRA_URL }}
          jira-user: ${{ vars.JIRA_USER }}
          merge-versions: "true"
