{{- with .Values.postgresql }}
{{- if .enabled }}
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ template "alfresco-content-services.database.repo.fullname" $ }}
  labels:
    {{- include "alfresco-content-services.database.repo.labels" $ | nindent 4 }}
spec:
  replicas: 1
  selector:
    matchLabels:
      {{- include "alfresco-content-services.database.repo.selectorLabels" $ | nindent 8 }}
  template:
    metadata:
      labels:
        {{- include "alfresco-content-services.database.repo.labels" $ | nindent 8 }}
    spec:
      containers:
        - name: postgres
          image: "{{ .image.repository }}:{{ .image.tag }}"
          imagePullPolicy: {{ .image.pullPolicy }}
          {{- $extConf := .primary.extendedConfiguration }}
          {{- if $extConf }}
          args:
            {{- range $line := splitList "\n" $extConf }}
            {{- if $line }}
            - "-c"
            - {{ $line | trim | quote }}
            {{- end }}
            {{- end }}
          {{- end }}
          {{- if .auth.existingSecret }}
          envFrom:
            - secretRef: 
                name: {{ .auth.existingSecret }}
          {{- else }}
          env:
            - name: POSTGRES_DB
              value: {{ .auth.database | default "alfresco" }}
            - name: POSTGRES_PASSWORD
              value: {{ .auth.password | default "alfresco" }}
            - name: POSTGRES_USER
              value: {{ .auth.username | default "alfresco" }}
          {{- end }}
          ports:
            - name: tcp-postgresql
              containerPort: {{ .primary.service.ports.postgresql | default 5432 }}
          livenessProbe:
            {{- toYaml .livenessProbe | nindent 12 }}
          resources:
            {{- toYaml .primary.resources | nindent 12 }}
{{- end }}
{{- end }}
