# Defines the deployment for the Alfresco Search (Solr) App
apiVersion: extensions/v1beta1
kind: Deployment
metadata:
  name: {{ template "alfrescosearch.fullName" . }}-solr
  labels:
    app: {{ template "alfrescosearch.fullName" . }}-solr
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
    component: search
spec:
  replicas: {{ .Values.alfrescosearch.replicaCount }}
  strategy:
    type: RollingUpdate
    rollingUpdate:
{{ toYaml .Values.global.strategy.rollingUpdate | indent 6 }}
  template:
    metadata:
      annotations:
        checksum/config: {{ include (print $.Template.BasePath "/config-search.yaml") . | sha256sum }}
      labels:
        app: {{ template "alfrescosearch.fullName" . }}-solr
        release: {{ .Release.Name }}
    spec:
    {{- with .Values.alfrescosearch.affinity }}
      affinity:
{{ tpl . $ | indent 8 }}
    {{- end }}
    {{- with .Values.alfrescosearch.tolerations }}
      tolerations:
{{ toYaml . | indent 8 }}
    {{- end }}
      {{- if .Values.global.alfrescoRegistryPullSecrets }}
      # only set this secret if a private docker registry variable is defined
      imagePullSecrets:
        - name: {{ .Values.global.alfrescoRegistryPullSecrets }}
      {{- end }}
      containers:
        - name: {{ .Chart.Name }}
          image: {{ template "alfrescosearch.dockerImage" . }}
          imagePullPolicy: {{ template "alfrescosearch.pullPolicy" . }}
          envFrom:
          - configMapRef:
              name: {{ template "alfrescosearch.fullName" . }}-solr-configmap
          ports:
            - containerPort: {{ template "alfrescosearch.internalPort" . }}
          resources:
{{ toYaml .Values.alfrescosearch.resources | indent 12 }}
          volumeMounts:
          - name: data
            mountPath: {{ .Values.alfrescosearch.persistence.search.data.mountPath }}
            subPath: {{ .Values.alfrescosearch.persistence.search.data.subPath }}
          readinessProbe:
            httpGet:
              path: /solr
              port: {{ template "alfrescosearch.internalPort" . }}
            initialDelaySeconds: {{ .Values.alfrescosearch.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.alfrescosearch.readinessProbe.periodSeconds }}
            timeoutSeconds: {{ .Values.alfrescosearch.readinessProbe.timeoutSeconds }}
          livenessProbe:
            httpGet:
              path: /solr/alfresco/admin/luke
              port: {{ template "alfrescosearch.internalPort" . }}
            initialDelaySeconds: {{ .Values.alfrescosearch.livenessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.alfrescosearch.livenessProbe.periodSeconds }}
            failureThreshold: 1
            timeoutSeconds: {{ .Values.alfrescosearch.livenessProbe.timeoutSeconds }}
          # sleep to drain any existing connections
          lifecycle:
            preStop:
              exec:
                command: ["/bin/bash", "-c", "sleep 10"]
      initContainers:
        - name: init-db
          image: busybox
          # command to allow solr to write to EFS volume. Details: https://issues.alfresco.com/jira/browse/DEPLOY-419
          command: ["sh", "-c", "chown -R 33007:33007 {{ .Values.alfrescosearch.persistence.search.data.mountPath }}"]
          volumeMounts:
          - name: data
            mountPath: {{ .Values.alfrescosearch.persistence.search.data.mountPath }}
            subPath: {{ .Values.alfrescosearch.persistence.search.data.subPath }}
      volumes:
      - name: data
        persistentVolumeClaim:
          claimName: "{{ .Values.alfrescosearch.persistence.existingClaim | default (printf "%s-solr-claim" (include "alfrescosearch.fullName" .)) }}"
