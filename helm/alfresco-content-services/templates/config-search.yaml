# Defines the properties required by the Alfresco Search (Solr) App
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "alfrescosearch.fullName" . }}-solr-configmap
  labels:
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    component: search
data:
  SOLR_ALFRESCO_HOST: "{{ template "content-services.shortname" . }}-repository"
  SOLR_ALFRESCO_PORT: "{{ .Values.alfrescosearch.repository.port }}"
  SOLR_SOLR_HOST: "{{ template "alfrescosearch.fullName" . }}-solr"
  SOLR_SOLR_PORT: "{{ .Values.alfrescosearch.service.externalPort }}"
  # The values for SOLR_CREATE_ALFRESCO_DEFAULTS and SOLR_OPTS, defined in the values.yaml for "search" are set next:
  {{- if .Values.alfrescosearch.environment }}
  {{- range $key, $val := .Values.alfrescosearch.environment }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
  {{- end }}

