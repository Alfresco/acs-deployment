# Defines the properties required by the Alfresco Search (Solr) App
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ template "alfresco-search.fullName" . }}-solr-configmap
  labels:
    heritage: {{ .Release.Service }}
    release: {{ .Release.Name }}
    chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    component: search
data:
  SOLR_ALFRESCO_HOST: "{{ .Release.Name }}-{{ .Values.alfresco-search.repository.host }}-repository"
  SOLR_ALFRESCO_PORT: "{{ .Values.alfresco-search.repository.port }}"
  SOLR_SOLR_HOST: "{{ template "alfresco-search.fullName" . }}-solr"
  SOLR_SOLR_PORT: "{{ .Values.alfresco-search.service.externalPort }}"
  # The values for SOLR_CREATE_ALFRESCO_DEFAULTS and SOLR_OPTS, defined in the values.yaml for "search" are set next:
  {{- if .Values.alfresco-search.environment }}
  {{- range $key, $val := .Values.alfresco-search.environment }}
  {{ $key }}: {{ $val | quote }}
  {{- end }}
  {{- end }}
