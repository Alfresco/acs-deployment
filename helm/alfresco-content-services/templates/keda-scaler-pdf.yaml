{{- if and .Values.keda.enabled (index .Values "alfresco-transform-service" "enabled") -}}
{{- $ctx := (dict "Values" .Values.keda "Chart" .Chart "Release" .Release) -}}
{{- $mqCtx := dict "Values" .Values.activemq "Chart" .Chart "Release" .Release }}
{{- $mqAdminPort := default "8161" (.Values.activemq.services.webConsole.ports).external.webConsole }}
{{- $atsCtx := (dict "Values" (index .Values "alfresco-transform-service") "Chart" .Chart "Release" .Release) }}
---
apiVersion: keda.sh/v1alpha1
kind: ScaledObject
metadata:
  labels:
    {{- include "keda.labels" $ctx | nindent 4 }}
  name: {{ printf "%s-tengine-pdfrenderer" (include "alfresco-content-services.fullname" $ctx) | trunc 63 | trimSuffix "-" }}
spec:
  scaleTargetRef:
    name: {{ template "alfresco-transform-service.pdfrenderer.name" $atsCtx }}
  triggers:
  - type: activemq
    metadata:
      managementEndpoint: {{ printf "%s-web-console:%v" (include "activemq.fullname" $mqCtx) $mqAdminPort }}
      destinationName: org.alfresco.transform.engine.alfresco-pdf-renderer.acs
      brokerName: {{ template "activemq.fullname" $mqCtx }}
      targetQueueSize: "10"
    authenticationRef:
      name: {{ printf "%s-activemq-auth-trigger" (include "alfresco-content-services.fullname" $ctx) | trunc 63 | trimSuffix "-" }}
  initialCooldownPeriod:  300
  cooldownPeriod:  900
  pollingInterval: 10
  idleReplicaCount: 0
  minReplicaCount:  1
  maxReplicaCount:  5
{{- end -}}
