---
suite: Activemq test persistence 
templates:
  - charts/activemq/templates/deployment-activemq.yaml
  - charts/activemq/templates/pvc-activemq.yaml
  - pvc.yaml
tests:
  - it: should render a volumeless deployment
    values: &testvalues
      - values/test_values.yaml
    set:
      persistence:
        enabled: false
    asserts:
      - isEmpty:
          path: spec.template.spec.volumes
        template: charts/activemq/templates/deployment-activemq.yaml
  - it: should render a deployment with set claim
    values: *testvalues
    set:
      persistence:
        enabled: true
        existingClaim: mysfsvolume
    asserts:
      - equal:
          path: >-
            spec.template.spec.volumes[0].persistentVolumeClaim.claimName
          value: mysfsvolume
        template: charts/activemq/templates/deployment-activemq.yaml
  - it: should render a deployment with dynamic claim name
    values: *testvalues
    set:
      persistence:
        enabled: true
        volumeMode: Block
        accessModes:
          - ReadWriteMany
          - ReadOnlyMany
    asserts:
      - equal:
          path: >-
            spec.template.spec.volumes[0].persistentVolumeClaim.claimName
          value: activemq-default-pvc
        template: charts/activemq/templates/deployment-activemq.yaml
      - isNull:
          path: spec.storageClassName
        template: pvc.yaml
        documentIndex: 1
  - it: should render a deployment with provided storage class
    values: *testvalues
    set:
      persistence:
        enabled: true
        storageClass: cheap
    asserts:
      - equal:
          path: >-
            spec.template.spec.volumes[0].persistentVolumeClaim.claimName
          value: activemq-cheap-pvc
        template: charts/activemq/templates/deployment-activemq.yaml
      - equal:
          path: spec.storageClassName
          value: cheap
        template: pvc.yaml
        documentIndex: 1
