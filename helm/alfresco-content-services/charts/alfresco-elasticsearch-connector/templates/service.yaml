apiVersion: v1
kind: Service
metadata:
  name: {{ include "alfresco-elasticsearch-connector.fullname" . }}
  labels:
    {{- include "alfresco-elasticsearch-connector.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alfresco-elasticsearch-connector.selectorLabels" . | nindent 4 }}
