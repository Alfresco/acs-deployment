{{- if and (eq .Values.global.tracking.auth "secret") (.Values.global.tracking.sharedsecret) }}
apiVersion: v1
kind: Secret
metadata:
  name: {{ template "alfresco-search.fullName" . }}-solr-secret
  labels:
    app: {{ template "alfresco-search.fullName" . }}-solr
    release: {{ .Release.Name }}
type: Opaque
data:
  JAVA_TOOL_OPTIONS: {{ include "java_tool_option" . | b64enc | quote }}
{{- end }}
