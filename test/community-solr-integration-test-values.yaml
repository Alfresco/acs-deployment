---
# Overlay on top of community-integration-test-values.yaml sizing alfresco-search (Solr)
# for the KIND integration runner.
alfresco-search:
  resources:
    requests:
      cpu: "0.01"
      memory: "512Mi"
    limits:
      cpu: "2"
      memory: "1.5Gi"
